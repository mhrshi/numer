package nm.droid.com.numericalmethods;

class MethodRow {

    int mPrimaryText;
    int mSubText;
    int mSupportingText;

    MethodRow(int primaryText, int subText, int supportingText) {
        mPrimaryText = primaryText;
        mSubText = subText;
        mSupportingText = supportingText;
    }
}
